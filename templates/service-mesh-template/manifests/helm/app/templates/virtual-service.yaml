apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: {{ .Values.service_namespace }}
  namespace: {{ .Values.service_namespace }}
spec:
  gateways:
    - istio-system/istio-ingressgateway
  hosts:
    - '*'
  http:
    - match:
        - uri:
            prefix: /{{ .Values.app_path }}
      rewrite:
        uri: /
      route:
        - destination:
            host: {{ .Values.app_service }}
            port:
              number: 8080
